export type ITodo = {
   id: string | number,
   title: string,
   isComplete: boolean,
}

/* export interface ITodo {  // another variant of pre-recording
   id: string | number,
   title: string,
   isComplete: boolean,
} */